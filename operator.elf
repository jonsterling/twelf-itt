operator : arity -> sort -> type.

op/ax : operator @0 val.

op/pair : operator (@0 , z , z) val.
op/spread : operator (@0 , s (s z)) use.

op/lam : operator (@0 , s z) val.
op/ap : operator (@0 , z) use.

op/inl : operator (@0 , z) val.
op/inr : operator (@0 , z) val.
op/decide : operator (@0 , s z , s z) use.
